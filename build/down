#!/usr/bin/env python
import argparse
from os.path import basename
from util.setup import shut_down_composite

parser = argparse.ArgumentParser(description='shut down docker containers')
parser.add_argument('system')
args = parser.parse_args()

shut_down_composite(basename(args.system))
